const config = {
    id: 'j-new-copy-range-id',
    styles: `
            position: "absolute";
            width: "100%";
            height: "1px";
            top: 0;
            overflow: "auto";
            z-index: -1;
            opacity: 0;
            filter: alpha(opacity=0);`
};

export default config;